student_grades = [9.1, 8.8, 7.5]
# dir(__builtins__) to check for built in functions
# help(function_name) to learn to execute
mysum = sum(student_grades)
length = len(student_grades)
mean = mysum/length
print(mean)